# Fixed-width layout specifications

These are sanitized year-specific field layouts for the 2018-2022 Core, Severity, and Hospital files. They contain field names, positions, lengths, and types only. No NRD observations, archives, linkage keys, or hospital identifiers are included. `nrd_build_cohort.py` reads the specifications from a private licensed project root during a full rerun.

