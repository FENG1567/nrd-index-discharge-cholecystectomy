# GitHub上传包报告

## 结论

已整理出一个可公开发布的复现仓库，包含聚合结果复现路径、授权数据全流程代码、代码本、年度布局规格、当前图片/表格参考文件、测试、CI、清单和校验说明。公开模式可以在没有NRD原始数据的情况下重建6个图名对应的PDF/PNG/SVG文件，并核对82,948例、A0 34,530例、A1 48,418例和校正后的8.700个百分点主要结果。

## 已纳入

- `README.md`和`README_zh-CN.md`：研究估计目标、解释边界、两种复现方式、限制和命令。
- `DATA_AVAILABILITY.md`、`CODE_AVAILABILITY.md`、`REPRODUCIBILITY.md`、`CHANGELOG.md`、`CITATION.cff`、MIT代码许可证、Git属性和忽略规则。
- `analysis/`：队列构建、分析、校正bootstrap、稳健性、组件标记和最终清单脚本。
- `codebook/`：已清理的临床代码本、证据表、ontology和2018-2022年Core/Severity/Hospital布局规格。
- `public_results/`：仅保留当前正文图表需要的聚合输入、校正主要CI摘要和数据字典。
- `figures/`：仅聚合结果图构建脚本。
- `outputs/figures/`：当前6个图各自的PDF/PNG/SVG参考文件。
- `outputs/tables/`：当前`table.docx`、`supplement.docx`和`figure_legends.docx`参考文件；未放入正文稿。
- `tests/`和`.github/workflows/ci.yml`：代码本、隐私、数值一致性和图片冒烟检查。

## 明确排除

未复制NRD压缩包、解压后的固定宽度记录、患者级表格、`NRD_VisitLink`、`KEY_NRD`、`HOSP_NRD`、医院标识、拟合的干扰模型预测、服务器中间文件、真实凭证、密码、服务器信息、运行日志和内部工作文件。也未复制正文稿和质量检查联系图。

这些排除不是缺失，而是公开发布的必要边界：NRD受HCUP授权条款约束；逐条记录和链接变量既不能公开，也不能通过聚合结果反推。

## 补齐内容

原有材料不足以直接构成一个可公开复现的GitHub仓库，因此补齐了：

1. 可移植的聚合图片构建脚本和6图18文件的输出约定；
2. 仅占位的凭证与原始输入布局模板；
3. 公开结果数据字典，明确每个CSV/JSON对应的图和表面板；
4. 公开模式和授权模式的README、运行编排脚本、依赖版本和环境说明；
5. 单元测试、隐私/小细胞检查、数值一致性测试、图片冒烟测试和CI配置；
6. 最后生成的文件清单和SHA-256校验文件。

## 已完成验证

- 10项代码本、数值一致性和图片冒烟单元测试全部通过。
- 仓库隐私与敏感信息扫描通过：未发现本地绝对路径、密码、服务器细节、真实凭证、受限标记、患者级数据文件、NRD压缩包、链接标识或未屏蔽的1-10小细胞。
- 公开聚合制图脚本成功生成6张图、每张3种格式，共18个非空文件。
- 在本项目最终绘图环境中，重建的6张600-dpi PNG与投稿参考PNG尺寸和像素逐一完全一致；PDF和SVG的可视内容由同一绘图调用生成，但文件哈希会因生成时间及SVG内部对象ID而变化。
- 主要数值核对通过：82,948、34,530、48,418、8.700及95% CI 8.287-9.080。
- 最终发布目录共有88个文件；`FILE_MANIFEST.csv`记录86个可发布实体（不收录自身和校验文件），`checksums.sha256`校验其余87个文件（不收录自身），87项哈希复核全部一致。

完整患者级复算仍必须由持有2018-2022年NRD许可数据的研究者在Linux服务器或WSL中运行；原生Windows PowerShell入口会主动拒绝启动需要POSIX伪终端的解压流程。这是运行环境及HCUP授权限制，不是公开包缺失。若本地缺少绘图库或完整统计依赖，验证会报告依赖问题；不应通过复制受限数据或关闭隐私检查来规避。

## 发布建议

GitHub仓库发布后，建议立即创建一个带版本号的release，并通过Zenodo关联仓库生成DOI。DOI应引用同一版本的代码、聚合结果、图表参考文件和校验清单；不要把NRD原始文件或本地凭证作为release附件。
